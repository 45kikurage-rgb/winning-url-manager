name: Revert default LINE label normalization

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/revert-line-label-normalization.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Restore previous default layout generation
        shell: python
        run: |
          from pathlib import Path
          path = Path('home-layout-edit.html')
          text = path.read_text(encoding='utf-8')
          old = "const defaultSource={...source,title:'LINE',novaFlags:(Number(source.novaFlags||0)&~2)};\n        insertCopy(db,columns,defaultSource,nextId++,null,Number(source.screen),Number(source.cellX),Number(source.cellY));"
          new = "insertCopy(db,columns,source,nextId++,null,Number(source.screen),Number(source.cellX),Number(source.cellY));"
          if old not in text:
              raise SystemExit('Target line not found; no file was changed.')
          path.write_text(text.replace(old, new, 1), encoding='utf-8')
      - name: Commit change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add home-layout-edit.html
          git commit -m "Restore pre-label-change layout behavior"
          git push
